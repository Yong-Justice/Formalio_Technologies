EXPO_PUBLIC_APP_ENV=development
EXPO_PUBLIC_API_BASE_URL=https://<project-ref>.supabase.co/functions/v1
EXPO_PUBLIC_SUPABASE_URL=https://<project-ref>.supabase.co
EXPO_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_xxx
EXPO_PUBLIC_EAS_PROJECT_ID=792092c6-07a1-4608-933f-bcc618880a62
EXPO_PUBLIC_ENABLE_AI=true
EXPO_PUBLIC_ENABLE_NOTCHPAY=false
EXPO_PUBLIC_ENABLE_MOMO_CSV_IMPORT=true

# Optional public monitoring value. Leave empty for MVP/dev builds.
EXPO_PUBLIC_SENTRY_DSN=

# Optional build-time Sentry source map upload values.
# Keep empty unless your CI/EAS environment is configured for Sentry releases.
SENTRY_AUTH_TOKEN=
SENTRY_ORG=
SENTRY_PROJECT=
SENTRY_RELEASE=

# Optional Android release signing values for local/CI Gradle builds.
# EAS managed credentials can be used instead, but release builds must never use
# the debug keystore for production distribution.
FORMALIO_UPLOAD_STORE_FILE=
FORMALIO_UPLOAD_STORE_PASSWORD=
FORMALIO_UPLOAD_KEY_ALIAS=
FORMALIO_UPLOAD_KEY_PASSWORD=

# Do not put Supabase service-role keys, OpenAI keys, payment secrets, or SMS
# provider tokens in the mobile app. Those belong in Supabase Edge Function
# secrets or another server-side secret manager.
